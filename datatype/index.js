let string = ""; // it's string
let number = 1; // it's number (int and float are the same here)
let boolean = true; // it's boolean
let nullValue = null; // it's null
let undefinedValue = undefined; // it's undefined
let arrayVal = []; // it's Array, but treated as object
let objectVal = {}; // it's Object val

let newVariable = "";
console.log(typeof newVariable); // output: string
